/**
 * @file
 * Global behaviors.
 */

(function ($, Drupal) {

  'use strict';

  /**
   * Global behaviours.
   */
  Drupal.behaviors.global = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (jQuery, Drupal));
